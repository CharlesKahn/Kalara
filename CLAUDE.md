## Git Commits
At natural stopping points after completing a set of related changes, ask: "Should I commit and push these changes to git?" If yes, run `git add -A && git commit -m "[descriptive summary]" && git push`.
